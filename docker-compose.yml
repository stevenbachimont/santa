version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "5001:5001"
    volumes:
      - ./server:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development

  client:
    build:
      context: .
      dockerfile: client/Dockerfile
    ports:
      - "3333:3333"
    volumes:
      - ./client:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development

volumes:
  node_modules:
